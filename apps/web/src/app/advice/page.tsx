import { PageShell } from "@/components/page-shell";

export default function AdvicePage() {
  return (
    <PageShell
      title="今日增长建议"
      description="每天告诉你下一步做什么。接入站点并完成首次审计后，这里会出现带证据的优先级卡片。"
    />
  );
}
