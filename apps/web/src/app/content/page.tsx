import { PageShell } from "@/components/page-shell";

export default function ContentPage() {
  return (
    <PageShell
      title="内容机会"
      description="内容缺口与答案型内容提示（含 GEO readiness 相关缺口）。"
    />
  );
}
